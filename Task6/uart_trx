module uart_trx (
    input clk,
    input [15:0] distance,
    output reg tx
);

    // Placeholder for UART TX implementation
    // You can replace this with a real UART FSM

    // For now, hold tx high
    initial tx = 1;

endmodule
